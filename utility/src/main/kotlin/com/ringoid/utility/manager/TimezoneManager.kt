package com.ringoid.utility.manager

import android.content.Context

class TimezoneManager(context: Context) {

    /* API */
    // --------------------------------------------------------------------------------------------
    fun getTimeZone(): Int = 0
}
