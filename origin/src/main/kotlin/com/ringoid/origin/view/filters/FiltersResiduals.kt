package com.ringoid.origin.view.filters

import com.ringoid.base.view.Residual

class LC_COUNTS(val countLikes: Int, val countMessages: Int,
                val totalNotFilteredLikes: Int, val totalNotFilteredMessages: Int) : Residual()
